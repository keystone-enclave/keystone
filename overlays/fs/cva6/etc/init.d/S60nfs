#!/bin/sh
#
# nfs        Skip NFS startup
#

umask 077

start() {
	echo "NFS preparation skipped, OK"
}

stop() {
	echo "OK"
}

restart() {
	stop
	start
}

case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  restart|reload)
	restart
	;;
  *)
	echo "Usage: $0 {start|stop|restart}"
	exit 1
esac

exit $?

